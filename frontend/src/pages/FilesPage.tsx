import styles from './PlaceholderPage.module.css';

export function FilesPage() {
  return (
    <div className={styles.page}>
      <h1>File manager</h1>
      <p className={styles.meta}>Phase 3 — API + upload UI</p>
    </div>
  );
}
